import React from 'react';

const Todo = props => {
  return (
    <React.Fragment>
      <h3>Todo</h3>
      <input type="text" placeholder="Todo"/>
        <button type="button">Add</button>
      <ul>
        
      </ul>
    </React.Fragment>
  );
};

export default Todo;
